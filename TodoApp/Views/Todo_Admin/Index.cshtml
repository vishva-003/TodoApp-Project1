@model IEnumerable<TodoApp.Models.Todo_Admin>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Admin_Title)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Admin_Description)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Admin_TaskDate)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Id)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Admin_Title)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Admin_Description)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Admin_TaskDate)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Id)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.Admin_Id">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.Admin_Id">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.Admin_Id">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
